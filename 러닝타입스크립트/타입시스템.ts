// let singer = "Aretha";

// let bestSong = Math.random() > 0.5 ? "Chain of Fools" : "Respect";

// let firstName = "Whitney";
// firstName.length();

// Error: This expression is not callable.

// 코드를 읽고 firstName이라는 변수를 이해한다.
// 초기값이 "Whiteney" 이므로 firstName이 string 타입이라고 결론 짓는다.
// firstName의 length 멤버를 함수처럼 호출하는 코드를 확인한다.
// string의 .length 멤버는 함수가 아닌 숫자라는 오류를 표시한다. 즉 함수처럼 호출 할 수 없다.]

// 구문 오류: 타입스크립트가 자바스크립트로 변환되는 것을 차단한 경우
// 타입 오류: 타입 검사기에 따라 일치하지 않는 것이 감지된 경우

// let firstName = "Carole";
// firstName = 1;

// let rocker: string;

// rocker = "Joan Jett";

// rocker.toUpperCase();

// rocker = 19.58;
// rocker.toPrecision(1);

// rocker.toUpperCase();

// let rapper = "Queen Latifah";

// rapper.length;

// rapper.push("!");

// let cher = {
//   firstName: "Cherilyn",
//   lastName: "Sarkisian",
// };

// cher.middleName;
